steps:
  - name: 'node:20'
    entrypoint: 'npm'
    args: ['install']

  - name: 'node:20'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install -g eas-cli
        eas login --username "nomadservice2" --password "Loneliness101"
        eas build --platform android --profile preview --non-interactive --clear-cache

options:
  logging: CLOUD_LOGGING_ONLY
